<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clloud.live.dao.UserDao">
    <select id="queryUser" resultType="com.clloud.live.entity.User">
        SELECT id, nickname, create_time, update_time
        FROM user
    </select>
    <select id="queryUserById" resultType="com.clloud.live.entity.User">
        SELECT id, nickname, create_time, update_time
        FROM user
        WHERE id=#{userId}
    </select>
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id" keyColumn="id"
        parameterType="com.clloud.live.entity.User">
        INSERT INTO user(id, nickname, create_time, update_time)
        VALUES (#{id}, #{nickname}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateUser" parameterType="com.clloud.live.entity.User">
        UPDATE user
        <set>
            <if test="nickname!=null">nickname=#{nickname},</if>
            <if test="updateTime!=null">update_time=#{updateTime}</if>
        </set>
        WHERE id=#{id}
    </update>
    <delete id="deleteUser">
        DELETE FROM
        user
        WHERE id=#{id}
    </delete>
</mapper>